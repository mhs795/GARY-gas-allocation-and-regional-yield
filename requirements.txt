pandas
numpy
pyomo
highspy            # default solver backend
# GLPK is the alternative backend (GARY_SOLVER=glpk). It is a system
# package, not a pip one:  sudo apt install glpk-utils
plotly
dash
diskcache
multiprocess
zstandard        # results-cache compression (falls back to stdlib gzip)
psutil
python-docx
openpyxl         # .xlsx reading: parameters workbook, field developments
xlsxwriter       # .xlsx chart output (openpyxl's chart axis XML is malformed)
